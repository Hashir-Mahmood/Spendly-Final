<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1234.Pages.Budgeting.AddSubscription"
             Title="AddSubscription"
             BackgroundColor="#f4f6f9">
    <ScrollView>
        <StackLayout Padding="20" Spacing="20">
            <Label Text="Subscription Name" FontSize="16" FontAttributes="Bold" TextColor="#444" />
            <Entry x:Name="NameEntry" Placeholder="Enter subscription name" BackgroundColor="White" TextColor="#444" />

            <Label Text="Price (£/month)" FontSize="16" FontAttributes="Bold" TextColor="#444" />
            <Entry x:Name="PriceEntry" Placeholder="Enter price" Keyboard="Numeric" BackgroundColor="White" TextColor="#444" />

            <Label Text="Renewal Date" FontSize="16" FontAttributes="Bold" TextColor="#444" />
            <DatePicker x:Name="RenewalDatePicker" BackgroundColor="White" TextColor="#444"/>

            <Label Text="Category" FontSize="16" FontAttributes="Bold" TextColor="#444" />
            <Entry x:Name="CategoryEntry" Placeholder="Enter category" BackgroundColor="White" TextColor="#444"/>

            <Label Text="Subscription Change (%)" FontSize="16" FontAttributes="Bold" TextColor="#444" />
            <Entry x:Name="SubscriptionChangeEntry" Placeholder="Enter change percentage" Keyboard="Numeric" BackgroundColor="White" TextColor="#444"/>

            <Button Text="Add Subscription"
                    Clicked="AddSubscriptionButton_Clicked"
                    BackgroundColor="#007AFF"
                    TextColor="White"
                    CornerRadius="10"
                    Padding="15"
                    FontAttributes="Bold"
                    FontSize="18" />
        </StackLayout>
    </ScrollView>
</ContentPage>