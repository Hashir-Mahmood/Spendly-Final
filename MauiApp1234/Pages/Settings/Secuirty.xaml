<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1234.Secuirty"
             BackgroundColor="#f4f6f9"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False">
    <Grid RowDefinitions="Auto,Auto,*,Auto">
        <ScrollView Grid.Row="2">
        <StackLayout Padding="20" Spacing="20">

            <!-- Header with Back Button and Title -->
            <Grid Margin="0,10,0,20" ColumnDefinitions="Auto,*,Auto">
                

                <!-- Title -->
                <Label Grid.Column="1" Text="Security" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>

                <!-- Placeholder to balance layout -->
                <Frame Grid.Column="2" CornerRadius="24" Padding="10" HasShadow="False" BackgroundColor="Transparent" IsVisible="False">
                    <BoxView WidthRequest="20" HeightRequest="20"/>
                </Frame>
            </Grid>

            <!-- Account Security -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Account Security" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- Change Password -->
                    <Button Text="Change Password" TextColor="#6f61ef" BackgroundColor="Transparent" BorderColor="#6f61ef" BorderWidth="1" CornerRadius="25" Padding="0,12" FontAttributes="Bold" Margin="0,5,0,10" Clicked="Button_Clicked"/>

                    <!-- Two-Factor Authentication -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,10">
                        <StackLayout Grid.Column="0">
                            <Label Text="Two-Factor Authentication" FontAttributes="Bold"/>
                            <Label Text="Extra layer of security for your account" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>

                    <!-- Biometric Authentication -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,10">
                        <StackLayout Grid.Column="0">
                            <Label Text="Biometric Authentication" FontAttributes="Bold"/>
                            <Label Text="Use fingerprint or face ID to login" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>

                    <!-- Important Notice -->
                    <Frame CornerRadius="8" Padding="12" BackgroundColor="#f0f0ff" Margin="0,10,0,0" HasShadow="False" BorderColor="Transparent">
                        <HorizontalStackLayout>
                            <Label Text="⚠" FontSize="16" TextColor="#6f61ef" VerticalOptions="Center" Margin="0,0,8,0"/>
                            <StackLayout>
                                <Label Text="Important" FontSize="14" FontAttributes="Bold" TextColor="#6f61ef"/>
                                <Label Text="Two-factor authentication requires a valid phone number" FontSize="12" TextColor="#6f61ef" Margin="0,2,0,0"/>
                            </StackLayout>
                        </HorizontalStackLayout>
                    </Frame>
                </StackLayout>
            </Frame>

            <!-- Login Security -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Login Protection" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- Remember Device -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,10">
                        <StackLayout Grid.Column="0">
                            <Label Text="Remember This Device" FontAttributes="Bold"/>
                            <Label Text="Stay logged in on this device" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>

                    <!-- Login Notifications -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,10">
                        <StackLayout Grid.Column="0">
                            <Label Text="Login Notifications" FontAttributes="Bold"/>
                            <Label Text="Get alerts when your account is accessed from a new device" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>

                    <!-- Suspicious Activity Alerts -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,0">
                        <StackLayout Grid.Column="0">
                            <Label Text="Suspicious Activity Alerts" FontAttributes="Bold"/>
                            <Label Text="Get notified of unusual account activity" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>
                </StackLayout>
            </Frame>

            <!-- Privacy Settings -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Privacy Settings" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- App Lock Timeout -->
                    <Label Text="App Lock Timeout" FontAttributes="Bold" Margin="0,5,0,5"/>
                    <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,15">
                        <Label Grid.Column="0" Text="30s" VerticalOptions="Center" FontSize="12" TextColor="Gray"/>
                        <Slider Grid.Column="1" Minimum="0" Maximum="4" Value="1" MinimumTrackColor="#6f61ef" MaximumTrackColor="#e0e0e0" ValueChanged="OnSliderValueChanged"/>
                        <Label Grid.Column="2" Text="10m" VerticalOptions="Center" FontSize="12" TextColor="Gray"/>
                    </Grid>

                    <!-- Data Collection -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,10">
                        <StackLayout Grid.Column="0">
                            <Label Text="Usage Analytics" FontAttributes="Bold"/>
                            <Label Text="Allow us to collect anonymous usage data" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White"/>
                    </Grid>

                    <!-- Location Sharing -->
                    <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,0">
                        <StackLayout Grid.Column="0">
                            <Label Text="Location-based Security" FontAttributes="Bold"/>
                            <Label Text="Detect unusual login locations" FontSize="12" TextColor="Gray"/>
                        </StackLayout>
                        <Switch Grid.Column="1" OnColor="#6f61ef" ThumbColor="White" IsToggled="True"/>
                    </Grid>
                </StackLayout>
            </Frame>

            <!-- Device Management -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Device Management" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- Show Current Device -->
                    <Frame CornerRadius="8" Padding="15" BackgroundColor="#f8f8f8" Margin="0,5,0,10" HasShadow="False">
                        <StackLayout>
                            <Label Text="Current Device" FontAttributes="Bold"/>
                            <Label Text="iPhone 15 Pro • London, UK" TextColor="Gray" FontSize="14" Margin="0,5,0,0"/>
                            <Label Text="Last login: Today, 14:32" TextColor="Gray" FontSize="12" Margin="0,5,0,0"/>
                        </StackLayout>
                    </Frame>

                    <!-- View All Devices Button -->
                    <Button Text="View All Devices" TextColor="#6f61ef" BackgroundColor="Transparent" BorderColor="#6f61ef" BorderWidth="1" CornerRadius="25" Padding="0,12" FontAttributes="Bold" Margin="0,5,0,10"/>

                    <!-- Log Out All Devices Button -->
                    <Button Text="Log Out of All Devices" TextColor="White" BackgroundColor="#FF3B30" CornerRadius="25" Padding="0,12" FontAttributes="Bold" Margin="0,5,0,0"/>
                </StackLayout>
            </Frame>

            <!-- Security Tips -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Security Tips" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- Tips -->
                    <Frame CornerRadius="8" Padding="15" BackgroundColor="#f0f0ff" Margin="0,5,0,10" HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*">
                            <Label Grid.Column="0" Text="🔒" FontSize="20" Margin="0,0,10,0" VerticalOptions="Start"/>
                            <Label Grid.Column="1" Text="Use a unique password for your financial accounts that you don't use anywhere else" TextColor="#6f61ef" LineBreakMode="WordWrap"/>
                        </Grid>
                    </Frame>

                    <Frame CornerRadius="8" Padding="15" BackgroundColor="#f0f0ff" Margin="0,5,0,10" HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*">
                            <Label Grid.Column="0" Text="🔑" FontSize="20" Margin="0,0,10,0" VerticalOptions="Start"/>
                            <Label Grid.Column="1" Text="Enable two-factor authentication on all your sensitive accounts" TextColor="#6f61ef" LineBreakMode="WordWrap"/>
                        </Grid>
                    </Frame>

                    <Frame CornerRadius="8" Padding="15" BackgroundColor="#f0f0ff" Margin="0,5,0,0" HasShadow="False">
                        <Grid ColumnDefinitions="Auto,*">
                            <Label Grid.Column="0" Text="📱" FontSize="20" Margin="0,0,10,0" VerticalOptions="Start"/>
                            <Label Grid.Column="1" Text="Keep your device's operating system and apps updated to the latest version" TextColor="#6f61ef" LineBreakMode="WordWrap"/>
                        </Grid>
                    </Frame>
                </StackLayout>
            </Frame>

            <!-- Contact Security Team -->
            <Frame CornerRadius="10" Padding="15" BackgroundColor="White">
                <StackLayout>
                    <Label Text="Security Support" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,0,0,15"/>

                    <!-- Report Issue Button -->
                    <Button Text="Report Security Issue" TextColor="White" BackgroundColor="#6f61ef" CornerRadius="25" Padding="0,12" FontAttributes="Bold" Margin="0,5,0,10"/>

                    <!-- Security Email -->
                    <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                        <Label Text="Email: " FontAttributes="Bold"/>
                        <Label Text="security@spendly.com" TextColor="#6f61ef"/>
                    </StackLayout>
                </StackLayout>
            </Frame>

            <!-- Version Info -->
            <StackLayout HorizontalOptions="Center" Margin="0,10,0,20">
                <Label Text="Security Protocol v2.1.3" TextColor="Gray" HorizontalOptions="Center"/>
                <Label Text="Last updated: 15 Apr 2025" TextColor="Gray" HorizontalOptions="Center"/>
            </StackLayout>

        </StackLayout>
    </ScrollView>
        <!-- Bottom Navigation -->
        <Grid Grid.Row="3" BackgroundColor="White" HeightRequest="60" ColumnDefinitions="*,*,*,*">
            <!-- return button -->
            <Button Text="←" 
FontSize="20" 
HorizontalOptions="Start" 
WidthRequest="40" 
HeightRequest="40" 
CornerRadius="20" 
BackgroundColor="Transparent" 
TextColor="Black" 
Padding="0" 
BorderWidth="0"
x:Name="ReturnButton"
Clicked="ReturnButton_Clicked"/>
        </Grid>
    </Grid>
</ContentPage>